ja:
  activerecord:
    models:
      user:
    
    attributes:
      user:
        authentication_key: "認証キー"
        
  time:
    formats:
      default: "%Y/%m/%d %H:%M:%S"
  ja:
  time:
    formats:
      default: "%Y/%m/%d %H:%M:%S"
      short: "%Y/%m/%d %H:%M"
